/* 

  ! Kono words jukto number k Number() dia convert korle seta NaN hobe.

  !NOTE: Kono string valute jodi only number thake tahole seta NUMBER function dia conversion hobe.... R jodi kono word jukto string k parseInt() dia conversion kora hoi tahole setar OUTPUT hobe only Numbers gulo.;

  ? Kono string k number a conversion korte hole obossoi seta k NUMBER string hote hobe "258422" amon type ar.

  TODO:: But akta important note holo ai j jekono word ba number Jodi string a convert kora hoi tahole seta typeof korle NUMBER i hobe and atar upor tar nijer soob nijosso function gulo kaj korbe.

  * Same STRING, NUMBER, BOOLEAN r Jonno 

*/

let num = 25;

let numToStr = String(num);

console.log(numToStr);

// console.log(numToStr.length);

// console.log(numToStr.charAt(0));




let str = "250d";
let strToNum = parseInt(str);

console.log(strToNum);


